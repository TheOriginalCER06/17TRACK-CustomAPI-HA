add_package:
  name: Add Package
  fields:
    tracking_number:
      required: true
      selector:
        text:

remove_package:
  name: Remove Package
  fields:
    tracking_number:
      required: true
      selector:
        text:

refresh_package:
  name: Refresh Package
  fields:
    tracking_number:
      required: true
      selector:
        text:

refresh_all_packages:
  name: Refresh All Packages
  fields: {}

add_package_from_helper:
  name: Add Package From Helper
  description: "Read the configured input_text helper and add the package value"
  fields: {}

remove_package_from_helper:
  name: Remove Package From Helper
  description: "Read the configured input_text helper and remove the package value"
  fields: {}
